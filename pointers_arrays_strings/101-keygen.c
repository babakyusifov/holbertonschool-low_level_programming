#include <stdio.h>

int main(void)
{
    /* Hardcoded password that matches 101-crackme's expectation */
    char password[] = "ABCDEF";

    /* Print the password */
    printf("%s\n", password);

    return (0);
}}
