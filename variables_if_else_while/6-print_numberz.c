#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0 (success).
 */
int main(void)
{
int i = 48;
for (; i <= 57; i++)
{
putchar(i);
}
putchar('\n');
return (0);
}
