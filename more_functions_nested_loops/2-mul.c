#include <stdio.h>

/**
 * mul - that multiplies two integers..
 * @a: The first integer.
 * @b: The second integer.
 * The result of multiplying a and b.
 */
int mul(int a, int b)
{
return (a * b);
}
